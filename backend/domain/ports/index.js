const { IAccountRepository } = require('./IAccountRepository');
const { ITransactionRepository } = require('./ITransactionRepository');

module.exports = { IAccountRepository, ITransactionRepository };
