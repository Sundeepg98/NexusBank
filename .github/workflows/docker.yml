name: Docker Build Test

on:
  push:
    branches: [master]

jobs:
  test-docker:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Test Docker build
        run: |
          cd backend
          docker build .
